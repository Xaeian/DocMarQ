[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "docmarq"
version = "0.3.0"
description = "DOCX generation library with fluent API"
readme = "readme.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{name = "Xaeian"}]
keywords = ["docx", "word", "ooxml", "document", "generation"]
dependencies = ["python-docx"]

[project.optional-dependencies]
math = ["matplotlib"]
md = ["PyYAML", "markdown-it-py", "mdit-py-plugins", "rlPyCairo", "svglib"]
all = ["PyYAML", "markdown-it-py", "matplotlib", "mdit-py-plugins", "rlPyCairo", "svglib"]

[project.urls]
Repository = "https://github.com/Xaeian/docmarq"

[tool.setuptools.packages.find]
include = ["docmarq*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_functions = ["*"]
